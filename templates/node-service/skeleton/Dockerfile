FROM node:24-alpine

WORKDIR /app

ENV NODE_ENV=production

COPY package*.json ./

RUN npm ci --omit=dev \
    && npm cache clean --force \
    && npm uninstall -g npm

COPY src ./src

USER node

EXPOSE 3000

CMD ["node", "src/server.js"]
